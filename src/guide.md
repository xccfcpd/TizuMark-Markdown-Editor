# TizuMark 使用说明

> 高效、优雅的 Markdown 编辑器 —— 从入门到精通

[TOC]

---

## 快速上手

### 界面一览

TizuMark 的界面分为四个主要区域：

| 区域 | 位置 | 说明 |
|------|------|------|
| **顶部工具栏** | 顶部 | 文件操作、格式插入、视图切换、主题、帮助 |
| **侧边栏** | 左侧 | 大纲导航与文件树（打开文件夹后） |
| **编辑区** | 中部 | CodeMirror 编辑器，Markdown 语法高亮、括号与引号自动配对 |
| **预览区** | 右侧 | 实时渲染 Markdown，同步滚动 |

### 基本操作

| 操作 | 方法 | 快捷键 |
|------|------|--------|
| 新建文件 | `文件 → 新建` | <kbd>Ctrl</kbd> + <kbd>N</kbd> |
| 打开文件 | `文件 → 打开`（支持批量选择） | <kbd>Ctrl</kbd> + <kbd>O</kbd> |
| 打开文件夹 | `文件 → 打开文件夹` | — |
| 保存文件 | `文件 → 保存` | <kbd>Ctrl</kbd> + <kbd>S</kbd> |
| 另存为 | `文件 → 另存为` | — |
| 最近打开 | `文件 → 最近打开` | — |
| 命令行打开 | `TizuMark.exe 文档.md` | — |
| 关闭标签 | 点击标签 × 或右键菜单 | <kbd>Ctrl</kbd> + <kbd>W</kbd> |

> 直接将 `.md` 文件**拖入窗口**即可打开，支持多文件拖放。`文件 → 最近打开` 可快速回到之前编辑的文档。

---

## 编辑器功能

### 视图模式

TizuMark 会根据文件类型自动选择最合适的视图，无需手动配置：

- **Markdown（`.md` 等 7 种扩展名）**：支持 **预览模式**（全屏渲染，适合阅读和演示）与 **编辑模式**（左写右览，所见即所得）。编辑模式下可点 **◄**（左侧）折叠编辑器、点 **►**（右侧）折叠预览，让另一侧独占屏幕；拖动中间分隔条可调整左右比例。
- **纯文本 / 代码（.txt 及非 Markdown 文本）**：自动进入**纯编辑视图**——只有编辑器、没有预览栏，也不会出现折叠 / 展开按钮（这类文件没有可渲染的预览）。
- **图片**：自动进入**预览（只读）视图**——只显示图片本身，不能切换到编辑。

> 若对纯文本点「预览」、或对图片点「编辑」，会弹提示：该文件类型不支持对应模式。

工具栏中间的两个标签可切换视图；<kbd>Ctrl</kbd> + <kbd>\\</kbd> 也可切换——它对任何文件都能按，但只有 Markdown 会真的切换，纯文本和图片会提示不支持该模式。在编辑模式下：

| 操作 | 方法 |
|------|------|
| 折叠编辑器 | 点击左侧 <kbd>◄</kbd> 按钮 |
| 折叠预览 | 点击右侧 <kbd>►</kbd> 按钮 |
| 调整比例 | 拖动中间分隔条 |

### 侧边栏：大纲与文件

点击 `视图 → 侧边栏` 可显示或隐藏侧边栏。侧边栏包含两个标签页：

- **大纲**：自动显示文档标题结构（H1 ~ H6），按层级缩进。**点击任意标题**，预览区自动跳转并居中；大纲随编辑实时更新。
- **文件**：当使用 `文件 → 打开文件夹` 打开一个目录后，这里会以树形结构展示该目录下的文件。点击文件即可在标签页打开；目录树会监听文件夹的外部增删变化并自动刷新。
- **文件树右键**：在文件 / 文件夹上右键，可进行新建文件夹 / 新建文件、重命名（<kbd>F2</kbd>）、复制、粘贴、删除、打开所在目录等操作。

### 多标签页

| 操作 | 方法 |
|------|------|
| 新建标签 | 点击标签栏 <kbd>+</kbd> 或 <kbd>Ctrl</kbd> + <kbd>N</kbd> |
| 切换标签 | <kbd>Ctrl</kbd> + <kbd>Tab</kbd> / <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>Tab</kbd> |
| 拖拽排序 | 直接拖拽标签调整顺序 |
| 关闭标签 | 点击 × 或 <kbd>Ctrl</kbd> + <kbd>W</kbd> |
| 右键菜单 | 关闭 / 关闭其他 / 关闭所有 / 复制路径 |
| 双击新建 | 标签栏空白处双击 |

已修改未保存的标签会显示 `*` 标记。

**会话恢复**：关闭 TizuMark 后重新打开，上次的标签页、文件夹工作区、展开的目录都会自动恢复，接续之前的工作状态。

### 查找与替换

查找相关的入口有四个，各司其职。**前两个是文档内查找**（分别在编辑区和预览区），**后两个是跨文件与文件速达**：

**编辑器查找替换**（<kbd>Ctrl</kbd> + <kbd>F</kbd>）：在当前文档的源码中查找，替换功能也在同一个面板里。

| 功能 | 说明 |
|------|------|
| 普通查找 | 输入关键词，点击下一个 / 上一个，并显示匹配数量 |
| 替换 | 输入新内容，点击替换或全部替换 |
| 区分大小写 | 精确匹配大小写 |
| 正则表达式 | 支持 JavaScript 正则语法 |
| 循环查找 | 到文档末尾后回到开头继续查找 |

**预览查找**（预览模式下 <kbd>Ctrl</kbd> + <kbd>F</kbd>）：在渲染后的内容中搜索并高亮定位。

**跨文件搜索**（<kbd>Ctrl</kbd> + <kbd>H</kbd>）：可在当前所有打开标签或指定目录中搜索，含正则与区分大小写选项。搜索结果按文件分组、显示行号列号与上下文片段，点击即跳转并在编辑区与预览区同时高亮。适合在大型项目文档中快速定位。

**文件搜索**（<kbd>Ctrl</kbd> + <kbd>P</kbd>）：在工作区中按文件名或路径快速跳转（VS Code 风格 Quick Open）。弹出面板后输入关键字即时筛选，匹配当前工作区内的 `.md` / `.markdown` / `.txt` 文件，↑/↓ 选择、<kbd>Enter</kbd> 打开、<kbd>Esc</kbd> 或点击面板外关闭；新建的文件在窗口重新获得焦点后会即时出现在列表中。

### 右键菜单

三处右键菜单，减少鼠标往返：

- **编辑区右键**：剪切 / 复制 / 粘贴 / 结构插入 / 文本格式 / 列表 / 链接与媒体 / 查找替换 / 全选
- **预览区右键**：复制 / 全选 / 复制为 HTML / 在预览中查找
- **标签页右键**：关闭 / 关闭其他 / 关闭所有 / 复制文件路径

### 外部文件变更提示

当打开的文件在 TizuMark 之外被其他程序修改时，顶部会弹出提示横幅，提供「重新加载」「忽略」「全部重新加载」「全部忽略」选项，避免误覆盖你的修改。

### 大文档保护

当文档超过约 **5000 行**或**约 400 万字符**时，预览会自动切换为滑动窗口模式，只渲染当前阅读区域附近的内容（约 1200 行的窗口），而非整篇一次性解析——因此数万行甚至更大的文档也能流畅打开，编辑区顶部会给出提示。

### 状态栏

窗口底部状态栏实时显示当前文档的**字数 / 字符数 / 行数**统计，以及光标所在的**行、列**位置，随编辑即时更新，方便随时掌握写作进度。

---

## 快捷键大全

> 下表为**出厂默认键位**。所有项均可在 **`文件 → 快捷键设置`** 中自定义、清除或恢复默认；内置 **Default / VSCode / Typora / Sublime Text** 四种预设方案，可一键切换、立即生效，无需重启。

### 文件与视图

| 快捷键 | 功能 | 快捷键 | 功能 |
|--------|------|--------|------|
| <kbd>Ctrl</kbd> + <kbd>N</kbd> | 新建文件 | <kbd>Ctrl</kbd> + <kbd>W</kbd> | 关闭标签页 |
| <kbd>Ctrl</kbd> + <kbd>O</kbd> | 打开文件 | <kbd>Ctrl</kbd> + <kbd>\\</kbd> | 切换视图（编辑 / 预览） |
| <kbd>Ctrl</kbd> + <kbd>S</kbd> | 保存文件 | <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>T</kbd> | 切换主题（明亮 / 暗黑） |
| <kbd>Ctrl</kbd> + <kbd>P</kbd> | 文件搜索（按名 / 路径跳转） | <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>P</kbd> | 导出 PDF |

### 查找与标签页

| 快捷键 | 功能 | 快捷键 | 功能 |
|--------|------|--------|------|
| <kbd>Ctrl</kbd> + <kbd>F</kbd> | 查找 / 替换 | <kbd>Ctrl</kbd> + <kbd>H</kbd> | 跨文件搜索 |
| <kbd>Ctrl</kbd> + <kbd>Tab</kbd> | 下一个标签页 | <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>Tab</kbd> | 上一个标签页 |

### 编辑与格式（选中文本后生效）

| 快捷键 | 功能 | 快捷键 | 功能 |
|--------|------|--------|------|
| <kbd>Ctrl</kbd> + <kbd>B</kbd> | 加粗 | <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>\`</kbd> | 行内代码 |
| <kbd>Ctrl</kbd> + <kbd>I</kbd> | 斜体 | <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>K</kbd> | 代码块 |
| <kbd>Ctrl</kbd> + <kbd>K</kbd> | 插入链接 | <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>Q</kbd> | 引用块 |
| <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>5</kbd> | 删除线 | <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>I</kbd> | 插入图片 |
| <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>M</kbd> | 数学公式 | <kbd>Alt</kbd> + <kbd>↑</kbd> | 上移当前行 / 选区 |
| <kbd>Ctrl</kbd> + <kbd>Enter</kbd> | 在下方插入行 | <kbd>Alt</kbd> + <kbd>↓</kbd> | 下移当前行 / 选区 |
| <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>Enter</kbd> | 在上方插入行 | | |

### 标题

| 快捷键 | 功能 |
|--------|------|
| <kbd>Ctrl</kbd> + <kbd>1</kbd> ~ <kbd>Ctrl</kbd> + <kbd>6</kbd> | 插入 H1 ~ H6 标题 |

### 撤销与重做

| 快捷键 | 功能 |
|--------|------|
| <kbd>Ctrl</kbd> + <kbd>Z</kbd> | 撤销 |
| <kbd>Ctrl</kbd> + <kbd>Y</kbd>（或 <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>Z</kbd>） | 重做 |

### 文档内导航与选择

| 快捷键 | 功能 | 快捷键 | 功能 |
|--------|------|--------|------|
| <kbd>Ctrl</kbd> + <kbd>Home</kbd> | 跳到文档开头 | <kbd>Shift</kbd> + <kbd>Ctrl</kbd> + <kbd>Home</kbd> | 从光标选到文档开头 |
| <kbd>Ctrl</kbd> + <kbd>End</kbd> | 跳到文档末尾 | <kbd>Shift</kbd> + <kbd>Ctrl</kbd> + <kbd>End</kbd> | 从光标选到文档末尾 |
| <kbd>Ctrl</kbd> + <kbd>←</kbd> | 按词左移光标 | <kbd>Shift</kbd> + <kbd>Ctrl</kbd> + <kbd>←</kbd> | 按词向左扩展选区 |
| <kbd>Ctrl</kbd> + <kbd>→</kbd> | 按词右移光标 | <kbd>Shift</kbd> + <kbd>Ctrl</kbd> + <kbd>→</kbd> | 按词向右扩展选区 |

### 字号缩放

| 快捷键 | 功能 |
|--------|------|
| <kbd>Ctrl</kbd> + 鼠标滚轮（编辑区或预览区） | 临时放大 / 缩小字号；约 3 秒无操作后自动写回设置 |

> **出厂未绑定键位的操作**（均可在 `文件 → 快捷键设置` 中自行指定，只是默认空着，避免与你的习惯冲突）：另存为、切换侧边栏、插入表格、表格插入行 / 列、无序列表、有序列表、任务列表、水平线、高亮标记、上标、下标、Mermaid 图表、目录、五种提示块（Note / Tip / Warning / Caution / Important）、关闭到托盘。它们都有对应的菜单 / 工具栏入口，想用键盘唤起时自行绑定即可。

---

## 格式工具栏

顶部下方的格式工具栏提供常用排版按钮（可点击右侧箭头折叠/展开）：

**直接按钮**：加粗、斜体、删除线、链接、图片、水平线、高亮、上标、下标。

**复合下拉**（悬停展开）：

- **结构插入**：行内代码、代码块、表格、引用块、数学公式、Mermaid 图表、目录 [TOC]
- **列表**：无序列表、有序列表、任务列表
- **标题**：H1 ~ H6
- **提示块**：Note / Tip / Warning / Caution / Important

---

## 插入功能详解

### 快捷插入（「/」命令）

在编辑器中输入「/」会弹出快捷插入列表，用方向键选择、回车即可插入常见语法块（标题、代码块、表格、提示块、公式、Mermaid、目录等），无需记忆语法。

- **打开**：在行首、或前一个字符是空格（含行中间的空格之后）输入 `/` 即弹出列表；继续输入关键字可实时过滤（如 `/table`、`/toc`、`/callout`）。
- **选择**：<kbd>↑</kbd> / <kbd>↓</kbd> 移动，<kbd>Enter</kbd> 插入，<kbd>Esc</kbd> 关闭。
- **自定义**：在 `文件 → 设置 → 快捷插入` 中可拖动调整各项顺序、隐藏不常用的项，让列表更贴合你的习惯。

### 结构插入

| 元素 | 插入内容 |
|------|----------|
| 标题 H1 ~ H6 | `#` ~ `######` 前缀标题 |
| 代码块 | ` ``` ` 包围的代码块（支持 JavaScript / Python / Rust / HTML / CSS / YAML / Shell 等语法高亮） |
| 表格 | 3×3 基础表格模板 |
| 引用块 | `>` 前缀引用段落 |
| 提示块 | Note / Tip / Warning / Caution / Important |
| 数学公式 | `$$` 包围的独立公式（KaTeX 渲染） |
| Mermaid 图表 | 流程图 / 时序图等模板 |
| 水平线 | `---` 分割线 |
| 目录 | `[TOC]` 标记，自动生成文档目录 |

> 行内公式使用 `$...$`，独立公式使用 `$$...$$`。公式内容会自动做 HTML 转义，`&`、`<`、`>` 等字符直接书写即可，无需手动写成 `&amp;` 这类实体。

> **化学式与化学方程式**（KaTeX mhchem 扩展）：用 `$\ce{...}$` 写化学式/反应式，`$\pu{...}$` 写物理单位，例如 `$\ce{2H2 + O2 -> 2H2O}$`、`$\ce{H2SO4}$`、`$\pu{123 kJ//mol}$`。反应箭头、上下标、同位素、离子电荷（`$\ce{SO4^2-}$`）等 mhchem 语法均可直接使用；多行反应式用 `$$` 块级公式并配合 `\\` 换行。
>
> **单位写法（siunitx 子集，内置）**：`$\si{...}$` 写单位（符号形式）、`$\SI{数值}{单位}$` 写带数值的完整量，例如 `$\SI{1.2e-3}{m s^-1}$`、`$\si{kJ//mol}$`。单位之间的**空格与 `.` 会渲染成细空格**（`$\si{kg m}$` → kg m），因此请按语义写：`kJ//mol` 是"每"（→ kJ/mol），`kg*m` 是乘法（→ kg·m），`m/s` 原样保留。常用单位宏（`\kilogram`、`\metre`、`\celsius`、`\percent` 等）也可直接写。

### 图表与可视化

用代码块即可画图（无需装任何插件），全部离线渲染、跟随明暗主题重绘、点击可放大：

| 代码块 | 引擎 | 内容写法 |
|--------|------|----------|
| ```` ```mermaid ```` | Mermaid | 流程图 / 时序图 / 甘特图 / 类图 / 状态图 / 饼图 / **脑图 mindmap** |
| ```` ```plantuml ```` | PlantUML → Mermaid | 时序图 / 用例图 / 组件图 / 类图 / 状态图 / 活动图 / 甘特图（`uml` / `puml` / `pu` 为别名）；不支持的分支（`fork` / `split` / `repeat` 等）会保留源码并提示 |
| ```` ```d2 ```` | D2 → Mermaid | D2 的节点/边语法与 `shape` / `style` 等常用属性 |
| ```` ```echarts ```` | ECharts | ECharts 的 option JSON（可选顶层 `tizuHeight` 指定画布高度，默认 360） |
| ```` ```wavedrom ```` | WaveDrom | WaveDrom 的 source JSON（`signal` / `assign` / `reg`；`wave` 为别名） |
| ```` ```dot ```` | Graphviz | DOT 语言描述的节点/边关系图，自动布局（`graphviz` / `gv` 为别名；首行写 `// engine: neato` 可切换布局引擎）；**中文节点名会自动补引号**，可直接写 `来料 --> 检验` |
| ```` ```tikz ```` | TikZ | `\draw` / `\fill` / `\node` 常用子集（`pgf` / `tikzpicture` 为别名）；`arc` / `.. controls` / `grid` 等未支持的路径语法会保留源码并提示 |
| ```` ```plot ```` | gnuplot | 函数绘图，如 `plot sin(x)`，支持 `set xlabel` / `set grid` 等（`gnuplot` 为别名） |
| ```` ```markmap ```` | Markmap | 缩进式 Markdown 大纲 → 思维导图 |

> 导出长图 / PDF / DOCX 时，这些图表会自动转成图片嵌入，无需额外处理。
> 语法写错时不会静默空白，而是显示失败原因并保留原始源码，便于就地修改。

### 文本格式

| 格式 | 语法 | 效果 |
|------|------|------|
| 加粗 | `**文本**` | **粗体文本** |
| 斜体 | `*文本*` | *斜体文本* |
| 删除线 | `~~文本~~` | ~~删除文本~~ |
| 行内代码 | `` `code` `` | `代码片段` |
| 高亮 | `==文本==` | ==高亮文本== |
| 上标 | `<sup>2</sup>` | x² |
| 下标 | `<sub>2</sub>` | x₂ |

### 列表

- 无序列表 —— `- ` 前缀
- 有序列表 —— `1. ` 前缀
- 任务列表 —— `- [ ] ` 前缀（预览中可勾选）

### 链接与媒体

- 链接 —— `[文本](URL)`，快捷键 <kbd>Ctrl</kbd> + <kbd>K</kbd>
- 图片 —— `![描述](图片URL)`

### 提示块（Callout）

TizuMark 支持 GitHub 风格的提示块，用于在文档中突出重要信息：

```markdown
> [!NOTE]
> 这是一个普通提示信息。

> [!TIP]
> 这是一个实用建议或技巧。

> [!WARNING]
> 这是一个需要关注的警告信息。

> [!CAUTION]
> 这是一个需要特别注意的风险提示。

> [!IMPORTANT]
> 这是一个至关重要的信息。
```

---

## 完整语法参考

[打开 Demo 文件，查看所有语法示例 →](demo.md)

---

## 图片管理

TizuMark 提供完善的图片处理能力，支持多种插入方式和自动去重。

### 插入图片

| 方式 | 操作 | 说明 |
|------|------|------|
| 粘贴 | <kbd>Ctrl</kbd> + <kbd>V</kbd> | 在编辑区粘贴剪贴板中的图片（截图、复制的图片文件等），按下方设定的存储方式保存后插入 |
| 插入对话框 | 格式工具栏「图片」按钮，或 <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>I</kbd> | 可选择本地文件，也可直接填入网络图片 URL |
| 右键菜单 | 编辑区右键 → 插入图片 | 同上 |

> 把图片文件**拖入窗口**是「打开」而不是「插入」——它会以只读预览的方式在新标签页中打开，不会插入当前正在编辑的文档。要插入，请用上面的粘贴或插入对话框。

### 自动去重

同一内容的图片只会存储一份。TizuMark 通过 **MD5 哈希**比对图片内容：

- 粘贴或插入图片时，自动计算文件 MD5
- 如果该图片已存在，直接复用已有文件
- 同名但内容不同的图片不会冲突

### 图片存储方式

可在 `文件 → 设置 → 图片存储方式` 中配置：

- **复制到 assets/（推荐）**：图片保存为独立文件，md 文件轻量，便于版本管理
- **Base64 嵌入**：图片编码到 md 文件内，单文件即可分享，但体积显著增大（约原图 1.4 倍）

### 图片存储路径

可在 `文件 → 设置 → 图片存储路径` 中配置：

- **相对路径**（默认）：相对于当前 Markdown 文件所在目录
- **绝对路径**：使用设定的固定目录（如 `D:\assets`）

设置下方会动态显示实际的引用路径示例，方便确认。

### 自动宽高属性

以「复制到 assets/」方式插入图片时，会自动附带原始宽高：

```html
<img src="assets/abc123.png" width="800" height="600" alt="示例图片">
```

你可以直接在编辑器中修改或删除 `width`/`height`：

- 改为百分比：`width="100%"`
- 完全删除：预览会按原始尺寸渲染

> 若存储方式选的是「Base64 嵌入」，插入的是 `![描述](data:image/png;base64,...)`，不会附带宽高属性。

### 图片查看器

预览中点击任意图片可弹出专用查看器，**鼠标拖拽**可平移，**滚轮以光标所在位置为锚点缩放**（想放大某个局部，就把鼠标移到那儿再滚），**双击**可重置回适应窗口的状态。无需离开 TizuMark 即可细看大图。

---

## 自动更新

TizuMark 启动时会在后台静默检查新版本：

| 场景 | 行为 |
|------|------|
| 启动时检查 | 静默执行，无弹框、无提示 |
| 发现新版本 | 自动弹出更新对话框，显示版本号、发布说明、下载按钮 |
| 已是最新 | 手动检查时弹框提示「已是最新版本」，下方为「确认」按钮，点击关闭（不弹 Toast）；启动时检查仍静默无提示 |
| 手动检查 | `帮助 → 检查更新`，有更新则弹框，无更新则弹框显示「已是最新版本」 |
| 更新失败 | Toast 提示"检查更新失败" |

更新对话框：

- 显示当前版本和新版本
- 展示发布说明（Markdown 渲染）
- 点击「下载更新」→ 下载进度条 → 「立即安装」→ 安装并重启
- 已是最新时：弹框保持打开，显示「已是最新版本」，下方仅一个蓝色「确认」按钮，点击即可关闭

---

## 导出功能

### 导出 HTML

`文件 → 导出 HTML` 生成独立的 HTML 网页文件：

- 包含完整 CSS 样式
- 表格、代码块、数学公式、Mermaid 图表均保留样式
- 可直接在浏览器中打开分享

### 导出长图

`文件 → 导出长图` 生成高清 PNG 长截图：

- 宽度固定 800px，高度自适应
- 支持暗黑主题样式
- 适合发布到社交媒体

### 导出 PDF

`文件 → 导出 PDF`（快捷键 <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>P</kbd>）通过系统打印对话框导出：

- 弹出提示确认后，使用浏览器原生打印功能
- 在打印对话框中可选择「另存为 PDF」、调整纸张方向/边距等
- Mermaid 图表会重新渲染以保证清晰度；数学公式沿用预览中已渲染的 KaTeX 结果，并内联其样式表

### 导出 DOCX

`文件 → 导出DOCX` 生成标准 `.docx` 文档（Word 2007+ 原生格式，可直接双击用 Word/WPS 打开编辑）：

- 由预览内容转换，**套用与预览一致的排版样式**（标题层级与分隔线、正文行距、列表、表格边框、引用块、代码块灰底、提示框配色、加粗/斜体、链接、图片等），尽量贴近预览界面显示效果
- 图片自动内联为文档内嵌资源（无需附带文件夹），并自动缩放以适配页面宽度，不会因过大被裁切显示不全
- Mermaid 图表在导出前会先渲染为 PNG 图片再嵌入，并自动缩放到页面宽度，避免 Word 无法识别 SVG 或因图过宽而显示不全
- KaTeX 公式会先渲染为 PNG 再嵌入，避免 Word 无法识别 MathML；万一渲染失败则保留 MathML 交给 Word 处理。代码块为兼容 Word 会去掉语法高亮 span、保留灰底与等宽字体
- 已知限制：提示框圆角/阴影、任务列表勾选框等 Word 不支持的 CSS 会被简化；代码块中过长的行可能超出页面边界

---

## 个性化设置

`文件 → 设置` 中可调整以下所有选项：

### 基本

| 设置项 | 可选值 | 默认值 | 说明 |
|--------|--------|--------|------|
| 界面语言 | 中文 / English | 中文 | 切换整个界面的显示语言 |
| 主题模式 | 明亮 / 暗黑 / 跟随系统 | 明亮 | 浅色或深色背景，或跟随操作系统 |
| 配色方案 | 基准 / 暖橙风 / 翠林风 / 极夜风 / 暮紫风 | 基准 | 整体 UI 配色风格 |
| 编辑器 / 预览 / 代码块字体 | 默认 / 系统字体（全量可搜索）/ 导入的自定义字体 | 默认 | 正文与代码块的字体族（设置 → 自定义字体） |

> 点击工具栏右侧的太阳/月亮图标可快速在「明亮 / 暗黑」之间切换。

### 编辑器

| 设置项 | 可选值 | 默认值 | 说明 |
|--------|--------|--------|------|
| 编辑字号 | 8 ~ 36px | 14px | 编辑器代码字体大小 |
| Tab 宽度 | 2 / 4 / 8 | 4 | 按 Tab 键插入的空格数 |
| 自动换行 | 开 / 关 | 开 | 代码超宽时自动换行 |
| 显示行号 | 开 / 关 | 开 | 编辑器左侧行号显示 |

### 预览

| 设置项 | 可选值 | 默认值 | 说明 |
|--------|--------|--------|------|
| 预览字号 | 8 ~ 36px | 16px | 预览区正文字体大小 |
| 行高 | 1.4 / 1.6 / 1.7 / 1.8 / 2.0 | 1.7 | 预览区正文行间距 |
| 最大宽度 | 无限制 / 800 / 1000 / 1200px | 无限制 | 预览区内容最大宽度 |
| 代码块行号 | 开 / 关 | 关 | 代码块是否显示行号 |
| 代码块自动换行 | 开 / 关 | 关 | 代码块内长行是否换行 |

### 行为

| 设置项 | 可选值 | 默认值 | 说明 |
|--------|--------|--------|------|
| 默认视图 | 预览 / 编辑 | 预览 | 启动时默认显示的视图模式（仅对 Markdown 生效；纯文本恒为编辑视图，图片恒为预览视图） |
| 滚动同步 | 开 / 关 | 开 | 编辑器滚动时预览是否同步 |
| 软换行（回车即换行） | 开 / 关 | 开 | 开启后段落内单个回车直接换行；关闭则恢复 CommonMark 标准（回车视为空格） |
| 图片存储方式 | 复制到 assets / Base64 嵌入 | 复制到 assets | 见上文「图片管理」 |
| 图片存储路径 | 相对 / 绝对 | 相对 | 见上文「图片管理」 |
| 关闭行为 | 每次询问 / 退出应用 / 最小化到托盘 | 每次询问 | 关闭最后一个窗口时的处理方式。最小化到托盘后可通过托盘图标重新呼出 |
| 显示系统托盘图标 | 开 / 关 | 开 | 在系统托盘中显示图标，可随时呼出窗口 |

### 自定义字体

`文件 → 设置 → 自定义字体` 区域：

- 点击「添加字体…」选择本地字体文件（`.ttf` / `.otf` / `.woff` / `.woff2`），导入后可反复使用
- 导入的字体会出现在「编辑器字体」「预览字体」下拉框中，可分别指定
- 下方提供字体预览，方便对比效果

---

## 常见问题

### 如何恢复默认设置？

`文件 → 设置` 或 `文件 → 快捷键设置` 中点击「恢复默认」。

### 支持哪些文件格式？

按打开方式分为三类：

| 类别 | 打开方式 | 格式 |
|------|----------|------|
| **Markdown 类** | 渲染预览，支持编辑 / 预览切换 | `.md` `.markdown` `.mdown` `.mkd` `.mkdn` `.mdwn` `.markdn` |
| **图片类** | 只读预览，不可编辑 | `.png` `.jpg` `.jpeg` `.gif` `.webp` `.bmp` `.svg` `.tif` `.tiff` `.ico` `.avif` `.heic` 等 20 种 |
| **明文 / 代码类** | 纯编辑视图（无预览栏），不做 Markdown 渲染 | `.txt` `.log` `.json` `.yaml` `.toml` `.csv` `.html` `.css` `.js` `.ts` `.jsx` `.py` `.rs` `.go` `.java` `.c` `.cpp` `.sh` `.sql` `.xml` `.tex` `.ipynb` 等 145 种 |

不在这份白名单内的后缀会提示「格式不支持」，并且不会被打开。

### 如何自定义快捷键？

`文件 → 快捷键设置`，点击「修改」后按下新组合键。点击「清除」可移除，点击「恢复默认」可还原。

### 预览区公式没有渲染？

先检查语法：行内用 `$...$`，独立公式用 `$$...$$`（需独占一行）。`&`、`<`、`>` 等字符可直接书写，软件会自动做 HTML 转义；若存在未闭合的 `$` 或非法的 LaTeX 命令，公式则不会渲染。

### 导出长图 / PDF 没有包含图片？

确保图片可访问。本地文件使用相对路径，网络图片确保网络连接正常。

### 如何打开整个文件夹批量管理文件？

`文件 → 打开文件夹` 选择目录后，侧边栏「文件」标签会以树形展示目录内容，点击即可打开，并在外部改动后自动刷新。

### 如何联系我们？

- **QQ 交流群：1035294939**
- 反馈建议、报告 Bug、交流使用心得

---

<p align="center">
  <b>TizuMark —— 让写作回归专注</b>
</p>
